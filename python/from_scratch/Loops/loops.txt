# for loop = a statment that will execute it's block of code
#            a limited amount of times
#
# while loop = unlimited
# for loop = limited

# Nested loop = the "inner loop" will finish all of it's iterations before
#               finishing one iter

# Loop control statement = change = loops execution from it's normal sequence

# break    = uesd to terminate the loop entirely,
# continue = skips to the next iteration of the loop,
# pass     = does nothing, acts as a placeholder

